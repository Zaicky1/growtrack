import 'package:flutter/material.dart';

class GTCommandPalette extends StatelessWidget {
  final Widget child;

  const GTCommandPalette({Key? key, required this.child}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return child; // placeholder for a command palette
  }
}
